import React from 'react';

// Importando estilos
import Tabela from '../componentes/Tabela';

export default function Inicio(){

    return(
        <div>
            <Tabela />
        </div>
    );
}